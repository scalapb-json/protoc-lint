language: scala
dist: xenial
sudo: false
jdk:
- oraclejdk8
- openjdk11
script:
- sbt "scalafmtCheck" "test:scalafmtCheck" "scalafmtSbtCheck" "+ compile" "+ test" testAll
cache:
  directories:
  - $HOME/.ivy2/cache
  - $HOME/.sbt/launchers
before_cache:
- find $HOME/.ivy2/cache -name "ivydata-*.properties" | xargs rm
- find $HOME/.ivy2/cache -name "*SNAPSHOT*" | xargs rm -rf
